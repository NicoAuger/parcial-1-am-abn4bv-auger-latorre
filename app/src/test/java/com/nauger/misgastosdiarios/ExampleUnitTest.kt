package com.nauger.misgastosdiarios

import org.junit.Test
import org.junit.Assert.*

/* Prueba unitaria local que se ejecuta en la máquina de desarrollo (no en un dispositivo Android).
   Sirve como ejemplo básico para validar la configuración del entorno de pruebas. */
class ExampleUnitTest {

    /* Verifica que la suma de 2 + 2 sea igual a 4. */
    @Test
    fun addition_isCorrect() {
        assertEquals(4, 2 + 2)
    }
}
